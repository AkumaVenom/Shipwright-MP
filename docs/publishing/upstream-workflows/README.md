# Original upstream workflow references

These original root workflow files are retained unchanged, but outside `.github/workflows` so they do not run when this snapshot is first published. They have not been adapted or executed for this unofficial vendored source. Their commands, runner versions, dependency retrieval, submodule assumptions and artifact/release behaviour need review before enabling. Do not copy them back wholesale and treat a queued run as a successful build.
