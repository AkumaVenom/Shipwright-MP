# No workflows enabled in this snapshot

Publishing this repository does not automatically build or release the game. Original root workflows are preserved under `docs/publishing/upstream-workflows` for maintainers to review and adapt. No successful GitHub Actions run or Windows build is claimed.
